<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.COMP2100Miniproject">
        <activity
            android:name=".PostCreateActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

        <activity
            android:name=".MessageReactionsActivity"
            android:exported="false"
            android:parentActivityName=".PostViewerActivity" />
        <activity
            android:name=".UserPostsActivity"
            android:exported="false"
            android:parentActivityName=".UserSearchActivity" />
        <activity
            android:name=".UserSearchActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PostViewerActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"> <!-- Added parentActivityName -->
            <!-- You can also add meta-data for older Android versions if needed -->
            <!--
                 <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
            -->
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>
    </application>

</manifest>